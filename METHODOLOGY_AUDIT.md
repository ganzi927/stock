# 방법론·구현 감사 — 2026-09-15~16

이 프로젝트는 일별 한국형 공포·탐욕지수, 옵션 OI/그릭스 기반 가정 모형, 선택적 AI 해설을 생성한다. 주문·체결·거래비용을 포함한 퀀트 매매 시스템은 아니다. 이번 수정은 확인 가능한 구현 오류와 근거 없는 해석의 제거이며, 수익성 또는 예측력 향상을 입증하지 않는다.

## 확인한 근거

아래 원문을 웹에서 열어 확인했다. 자료가 직접 설명하는 사실과 이번 코드에 대한 감사 판단을 구분한다.

| 출처 | 원문이 뒷받침하는 사실 | 코드에 적용한 판단 |
|---|---|---|
| [CME: Open Interest](https://www.cmegroup.com/education/courses/introduction-to-futures/open-interest) | OI는 미청산 계약이고 거래량은 거래된 계약이다. 포지션 종료 전까지 OI가 유지된다. | 당일 거래량이 작다는 이유만으로 OI를 폐기하는 근거는 없다. 총 OI와 순매매를 딜러의 기존 순포지션과 동일시할 수 없다. |
| [OCC/OIC: Gamma](https://www.optionseducation.org/advancedconcepts/gamma) | 옵션 매수 포지션의 감마는 콜/풋 모두 양수, 매도는 음수다. 감마는 기초자산 변화에 대한 델타 변화다. | 콜 Wall 돌파만으로 매수 헤지·가격 가속을 추론한 설명을 제거했다. 델타중립 헤지라면 헤지 변화는 옵션 델타 변화의 반대지만, 실제 딜러 포지션은 별도 입력이 필요하다. |
| [OCC/OIC: Black-Scholes Formula](https://www.optionseducation.org/advancedconcepts/black-scholes-formula) | 옵션 모델의 입력과 이상화된 모형 가정을 설명한다. | 수학 테스트는 모형 구현 일치만 검증한다. 현실 가격이나 실제 터치확률의 정확성을 보장하지 않는다. |
| [SciPy: spearmanr](https://docs.scipy.org/doc/scipy/reference/generated/scipy.stats.spearmanr.html) | 상수 입력의 상관은 정의되지 않으며 NaN이다. | 상수/비유한 통계를 유효 CI 결과로 해석하지 않도록 했다. |
| [arch: Time-series Bootstraps](https://bashtage.github.io/arch/bootstrap/timeseries-bootstraps.html) | 시계열 bootstrap은 연속 블록을 재표집하며 길이 선택이 필요하다. | 레짐에 맞는 날만 압축하지 않고 원 거래일 축과 마스크를 함께 재표집한다. 이 변경 자체가 정상성이나 CI coverage를 입증하지는 않는다. |
| [Bailey et al.: The Probability of Backtest Overfitting](https://www.davidhbailey.com/dhbpapers/backtest-prob.pdf) | 동일 표본의 반복적 전략 선택은 표본외 성과를 과대평가할 수 있다. | 단일 탐색 CI나 다른 리포트와의 근접도를 전략 검증으로 취급하지 않는다. 고정 결론을 제거하고 미사용 표본외 검증 필요성을 명시했다. |
| [Anthropic API reference](https://platform.claude.com/docs/en/api/completions/create) | temperature 0도 완전 결정론을 보장하지 않는다고 명시한다. | 재호출 바이트 동일성 보장이라는 문서 주장을 정정했다. 모델 버전/입력/출력 보관이 별도로 필요하다. |

이 자료들은 이 프로젝트의 파라미터 252일, 20일 평활, ±30% 범위, 딜러 콜 롱·풋 숏 가정이 한국 시장에 최적이라는 증거가 아니다.

## 수정한 문제

| 문제 | 수정 | 검증 |
|---|---|---|
| `outlook._fmt_fgi`가 투심 점수 방향을 반대로 전달 | 낮음=평활 변동성/P-C가 과거 대비 높음으로 정정 | 프롬프트 회귀 |
| 퍼센트 IV를 소수용 5.0으로 clip하여 20/30% 사이 보간도 5%로 왜곡 | 기존 소수 bounds를 퍼센트 단위 0.1~500으로 환산 | log 대칭 행사가의 25% 보간 |
| `_d1_d2`만 T/sigma를 floor하고 나머지 공식은 원값 사용 | 양수·유한 입력 검증, 부분 floor 제거 | 극소 양수의 analytic gamma 및 잘못된 입력 |
| 미래 만기가 없을 때 만료 체인 재사용 | 지수·개별주 모두 빈 체인 반환 | 만기 당일/전일 API fixture |
| sticky-moneyness가 비대칭 탐색 그리드 중앙을 실제 spot으로 사용 | 관측 spot 명시 전달 | 관측 spot에서 sticky/fixed 일치 |
| 거래량 대비 OI가 크면 유효 계약 삭제 | 근거 없는 OI 삭제 제거 | 거래량 0·큰 OI 계약 |
| 부분 체인 MaxPain만 제공 | 기존 windowed 근사 유지 + 전 행사가 값 추가 | 독립 내재가치 최소점 fixture |
| 전체 부호 반전 시 콜 Wall 소실 | 타입별 절대 GEX 합의 집중점 사용 | 전역 부호 반전 |
| 등락 한쪽이 없는 날 breadth가 NaN | 한쪽 합=0, 양쪽 총합=0이면 NaN | 전 종목 상승/하락/보합 |
| NaN이 백분위 분모에 포함되고 최신 결측도 점수 산출 | 유한 관측만 순위 분모에 사용, 현재 결측은 NaN | 상수/최댓값/결측 fixture |
| 과거 리포트 IV 백분위에 최신 미래 캐시 결과 사용 | 캐시는 보존하되 as_of까지로 계산 제한 | 미래 행 추가 전후 불변 |
| 백분위 점수에 독립 이항식 95% CI 표시 | 검증된 추정법 선택 전 CI 미표시 | score_ci=None |
| 백테스트의 날짜 압축·최장 horizon 일괄 삭제 | 원 축 유지, horizon별 결측 처리 | 마스크/표본 수 fixture |
| 상수 IC/무효 bootstrap·n/h 오해 | 정의 불가 처리, 유효 반복 수 기록, n/h는 참고 블록 수 | 상수/마스크 fixture |
| 실행 결과와 무관한 백테스트 고정 결론 | 탐색 통계의 해석 한계만 출력 | 보고서 합성 실행 |
| OI 쏠림=방향성, Wall=헤지 가속, MaxPain=수렴 | 계산 의미·관찰 레벨로 정정 | 코드/프롬프트 검토 |
| 숫자 검증 실패 AI 해설도 경고 후 노출 | 검출 시 종합 해설 게시 중단 | 잘못된 숫자 주입 |

기존 `spec/`의 충돌하는 과거 설명에는 감사 정정 절을 추가했다. 과거 README/RECONCILIATION/WORKPLAN/VALIDATION 기록은 새로운 실증 결과로 읽으면 안 된다.

## 아직 확정하지 않은 부분

사용자가 추측에 따른 변경을 금지했으므로 아래 모델·운영 정책은 새 값으로 임의 교체하지 않았다.

1. **딜러 부호**: 기존 콜 롱·풋 숏 가정은 실제 재고 데이터로 확인되지 않았다. `sign_overrides`는 여전히 기본 비활성. 사용자 선택지: 가정별 민감도를 따로 표시하거나, 검증된 포지션 없을 때 signed exposure/Zero Gamma를 숨기는 엄격 모드. 현재 r/q/IV 밴드는 이 인벤토리 불확실성을 포함하지 않는다. 일부 섭동에서 영점이 없어지는 경우도 현 밴드만으로는 충분히 드러나지 않는다.
2. **IV 보간/외삽**: 선형 윙 외삽·단일 관측 복사·중앙값 대체는 기존 가정이다. bounds 환산은 단위 오류 수정일 뿐 무차익 스마일 검증이 아니다. 원본/보간 여부와 OI 가중 커버리지를 표시하고, 검증 안 된 IV가 필요한 경우 계산 중단할지 결정해야 한다.
3. **배당·r/q**: `dividends.py`는 근사 금액·미래 날짜를 하드코딩한다. 공시 식별자/공개시점이 없어 과거 시점에 알려진 배당인지 검증할 수 없다. 배당락일과 실제 지급일의 할인도 구분해야 한다. 지수의 연말 단일 배당 캘린더, 상수 r/q, 개별주 상수 q는 확인된 시장 입력으로 간주할 수 없다. 이번에 임의 금액을 넣지 않았다.
4. **만기 캘린더**: 월물/목요일을 요일 계산으로 파싱하며 휴장 예외를 반영하는 공식 캘린더가 연결돼 있지 않다. 이번 T-0 필터 수정은 이 문제까지 해결한 것이 아니다. 공식 최종거래일 데이터 계약 또는 검증된 휴장 캘린더 연계가 필요하다.
5. **데이터 정합**: 원시 주가의 기업행동 조정, 실제 KOSPI200 구성종목, 소스별 공개시점/수정 이력, 지표별 stale 여부가 보장되지 않는다. `latest_result`는 최신 유효 점수를 과거에서 가져올 수 있고 헤드라인/추이의 warm-up 자격도 완전히 통일돼 있지 않다. 이를 처리할 허용 지연·결측 정책이 필요하다.
6. **백테스트**: 현재 것은 종가 기준 IC 분석이다. 당일 EOD 자료를 보고 당일 종가로 거래할 수 있다는 뜻이 아니다. 실제 매매 대상, 신호 확정 시각, 다음 체결 시각, 보유기간, 비용, 표본외 기간, 다중검정 대상 전략 집합을 먼저 고정해야 한다. 7/7 필터를 출시 6개 조합에도 쓰는 비교 구조 및 레짐 전체기간 중앙값은 탐색용으로 남겨뒀다.
7. **PoT**: 상수 ATM IV GBM에서 만기까지의 first-passage 확률이다. 내일 확률/실제 확률/서로 배타적인 시나리오 확률이 아니다. 스큐·점프·이산 배당 영향은 미검증이다.
8. **AI**: 숫자 검사로 모든 환각을 막을 수 없다. 현재 정규식은 100 이상 숫자 중심이고 허용 오차·단위·종목 혼동 한계가 있다. 전면 구조화 출력/결정론 템플릿 전환 여부는 별도 선택이다.

## 검증 기록

- `python tests/test_math.py`: 기존 수학 검사 25개 PASS. 유한차분 그릭스, 패리티, PoT 근사 MC, EMA 등.
- `python -m unittest discover -s tests -p test_audit.py -v`: 감사 회귀 테스트 15개 PASS.
- 실데이터 `backtest.py` 및 API를 사용하는 일일 통합 리포트: 미실행. 저장소에 운영 캐시 및 인증 설정이 제공되지 않았다.
- 테스트는 외부 API fixture/합성 자료로 수행했다. KRX 원시 데이터 필드의 정확성, 휴일 실제 만기, 현실 수익률을 인증한 결과가 아니다.
- 입력이 비정상이면 그릭스가 명시적으로 ValueError를 낸다. 이전처럼 잘못된 수치/NaN 합계를 정상 결과로 내는 것보다 오류가 드러나지만, 운영에서는 오류 처리·알림을 함께 검토해야 한다.

재현 명령:

```bash
python -m pip install -r requirements.txt
python tests/test_math.py
python -m unittest discover -s tests -p test_audit.py -v
```
