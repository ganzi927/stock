# 품질 게이트 버전 사용 안내

## 실행

기존 일일 실행 방식은 유지한다.

```bash
python -m pip install -r requirements.txt
python combined_main.py --as-of 2026-09-15 --force
```

위 날짜는 실행 형식 예시다. 실제 원하는 기준일을 입력한다. 운영 API 키/시세 캐시가 필요하며 이번 ZIP에 인증정보나 실데이터는 포함하지 않았다.

## 달라진 기본 화면

- 공포탐욕지수 앞에 지표별 출처, 실제 기준일, 상태, 의존 입력, 합성 포함/제외 사유가 표시된다.
- 판단용 맥락의 합성 후보는 Momentum, 실제 VKOSPI 기반 Volatility, 실제 정규 옵션 거래량 기반 Put/Call이다. 이 셋도 같은 거래일·유한 값·기존 252개 유효 점수 워밍업 조건을 충족해야 한다.
- Momentum만 남을 수도 있으며 이 경우 사용 구성에 Momentum만 표시한다. 결측을 0점으로 채우지 않는다. TOTAL은 종전 6개 구성과 직접 비교하면 안 된다.
- Strength/Breadth는 실제 구성종목·기업행동 검증 한계, Safe Haven은 합성 채권/분배금 조정 한계 때문에 연구용으로 보존된다. Credit Spread는 기존대로 합성 제외 매크로 맥락이다.
- 옵션은 관측 OI/거래량 요약을 먼저 보여준다. Zero Gamma, signed exposure, Wall, MaxPain, PoT 등 모델 결과는 접힌 연구용 영역에 들어간다. 기본 종합 AI 입력에는 포함되지 않는다.
- 관측/연구용 구분은 데이터와 사용 범위의 구분이며 예측력 인증이 아니다. 현재 승인된 매매전략은 없다.

## AI

- 기존 `KFGI_AI=0`이면 모든 AI 요청을 중단한다.
- 기본 `KFGI_RESEARCH_AI=0`: 연구용 옵션 AI 요청을 하지 않는다.
- `KFGI_RESEARCH_AI=1`: 연구용 옵션 내부에서만 별도 해설을 생성한다. 기본 종합 해설로 병합하지 않는다.
- 키가 없으면 AI를 생략한다. 기본 종합 입력은 허용 필드만 직렬화하므로 research 필드를 덧붙여도 들어가지 않는다.
- 자유문장 해설의 완전한 사실 검증을 보장하지 않는다. 기존 숫자 검사에도 범위/단위 한계가 있다.

## 공식 기준자료

`reference_data.json`은 의도적으로 빈 목록으로 제공한다. 공식 출처를 갖는 정확한 개별 기록을 확보하지 못했으므로 추정 날짜·금액을 넣지 않았다. 현재는 자동 수집기가 아니라 **검토된 기록을 읽는 로더**다.

루트 필드:

```json
{"schema_version": 1, "expiries": [], "dividends": [], "dividend_coverage": []}
```

각 레코드는 `source`(공식 HTTPS URL), `available_at`(타임존 포함 ISO 공개시각), `verification_note`(확인한 문서/항목)를 갖는다. 출처 도메인 검사만으로 문서 내용이 자동 검증되는 것은 아니다. 정확한 수치와 공개시각을 원문에서 확인해 입력해야 한다. 날짜만 알고 시간을 모르면 시간을 추정해서 넣지 않는다.

| 목록 | 추가 필수 필드 | 의미 |
|---|---|---|
| expiries | product, nominal_expiry, expiry | 상품명, 요일 계산 명목 만기, 공식 최종거래일(각 YYYY-MM-DD) |
| dividends | name, event_id, ex_date, pay_date, amount | 종목명, 수정 이력에서 유지할 ID, 배당락일, 지급일, 주당 원화 배당액 |
| dividend_coverage | name, start, end, complete=true | 해당 구간의 배당 일정이 완전하다는 검토 기록. 자료가 없다고 true로 쓰면 안 됨 |

`product`는 코드의 상품명과 일치해야 한다: `코스피200 옵션`, `코스피200 위클리(목) 옵션`, `삼성전자 옵션`, `SK하이닉스 옵션`.

- 기록의 공개시각이 기준일 한국시간 종료 이후이면 사용하지 않는다. 이는 일별 연구 cutoff이고 실제 종가 체결 가능시각이 아니다.
- 확정 만기가 있으면 체인 만기를 정정한 뒤 T-0/만료 필터를 적용한다. 없으면 명목 만기를 연구용으로 유지하며 휴일 미확인 사유를 표시한다.
- 개별주 배당이 완전성 조건을 충족하면 배당락일로 해당 옵션 기간의 이벤트를 고르고 실제 지급일까지 할인한다. q로 환산하는 모델 자체는 추정이며 계속 연구용이다.
- 불완전한 배당 목록으로는 q를 확정하지 않는다. 종전 종목별 상수 q는 연구용으로만 남기고 미확인 사유를 붙인다. `dividends.py`의 하드코딩 개별주 배당은 운영 계산에서 더 이상 참조하지 않는다.
- 지수의 종목별 전체 배당 바스켓/할인곡선은 확보되지 않았다. 지수 r/q와 배당락 휴리스틱은 연구용 가정이다.

## 로컬 데이터 점검

```bash
python validate_inputs.py --as-of 2026-09-15
```

`reports/input-quality.json`에 소스별 파일 해시, 기준일까지 행 수, 미래 제외 행 수, 중복 키, 결측·비유한 값, 실제 최신일을 기록한다. 네트워크 요청은 하지 않는다. 누락/불량/지연 캐시가 있으면 종료코드 2, 기본 캐시 구조 검사 통과는 0이다. 0은 기준자료 완전성·예측력·공개시각 검증 성공을 뜻하지 않는다. `backtest_eligible=false`를 별도로 기록한다.

통합 리포트 `.meta.json`에도 `quality_policy_version=2`와 지표별 상태가 저장된다. 구버전 메타 파일로는 멱등 종료하지 않으므로 새 정책으로 다시 생성된다. HTML이 존재한다는 사실과 모든 연구용 모델이 계산됐다는 것은 다르다.

## 검증 범위

```bash
python tests/test_math.py
python -m unittest discover -s tests -p 'test_*.py' -v
```

테스트의 공식 도메인 URL·배당·휴일 수치는 **합성 fixture**이며 운영 기준자료로 복사하지 않는다. 실데이터 접근 없이 재현 가능한 테스트다. 기존 backtest.py는 옛 연구용 구성의 탐색 IC로 남기고 현재 판단용 구성의 성과 검증이 아님을 출력한다.

매매 대상, 보유기간, 체결시각, 비용, 미사용 검증기간은 정하지 않았다. 이를 확정하기 전 실제 전략 성과를 만들거나 주장하지 않는다.
